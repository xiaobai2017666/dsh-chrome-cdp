node_modules

# build outputs (tsdown): generated by `npm run build`
# anchored to the repo root — a bare "client"/"lib" would also ignore
# src/client/ (the panel sources) at any depth and break git installs
/lib
/client

# diagnostics / scratch
*.log
