node_modules

# build outputs (tsdown) are COMMITTED and shipped in the git repo:
# pnpm git installs then need no prepare/build script, no allowBuilds gate.
# (src/client/ must never be ignored — a bare "client" pattern did that.)

# diagnostics / scratch
*.log